# oapi-codegen configuration. Regenerate with `make generate`.
package: api
# Path is relative to the directory `go generate` runs in (internal/web).
output: api/api.gen.go
generate:
  models: true
  std-http-server: true
  strict-server: true
  embedded-spec: true
output-options:
  skip-prune: false
